app = "tcc"
primary_region = "iad"
kill_signal = "SIGINT"
kill_timeout = 5

[env]
  ASPNETCORE_ENVIRONMENT = "Production"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80
